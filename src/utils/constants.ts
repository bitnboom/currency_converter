const INPUT_NAMES = {
	FROM: 'FROM',
	TO: 'TO',
};

const DEFAULT_CURRENCIES = {
	USD: 'USD',
	RUB: 'RUB',
	EUR: 'EUR',
};

const SUPPORTED_LANGS = {
	en: 'en',
	ru: 'ru',
};

export { INPUT_NAMES, DEFAULT_CURRENCIES, SUPPORTED_LANGS };
